# Zeus Releases

Binary releases for Zeus daemon.

## Install

```bash
curl -sL https://github.com/piotrga/zeus-releases/releases/latest/download/zeus-installer -o /tmp/zeus && chmod +x /tmp/zeus && sudo /tmp/zeus <name>
```

Replace `<name>` with the child's name (e.g., `peter`).
