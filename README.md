# Zeus Releases

Binary releases for Zeus daemon.

## Install

```bash
curl -sL https://github.com/piotrga/zeus-releases/releases/download/v1.0.0/zeus-installer -o /tmp/zeus && chmod +x /tmp/zeus && /tmp/zeus <name>
```

Replace `<name>` with the child's name (e.g., `peter`).
